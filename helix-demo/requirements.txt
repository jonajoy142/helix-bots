# Helix Unified Backend Requirements (runtime / Vercel serverless)
# Merged from whatsapp-support-bot-langgraph, ai-lead-qualification-agent, and multichannel-chatbot-platform
# Keep this file minimal: everything here is bundled into the Vercel function,
# which has a hard 500 MB limit. Local-only tooling belongs in requirements-dev.txt.

# FastAPI and server
fastapi==0.115.0
python-dotenv==1.0.1
requests==2.32.3

# LangChain and LangGraph
langchain==0.3.3
langchain-openai==0.2.2
langchain-community==0.3.1
langgraph==0.2.45
langgraph-checkpoint-sqlite==2.0.1

# Vector database and embeddings
faiss-cpu==1.8.0

# Data validation
pydantic==2.9.2

# Google Sheets integration (optional)
gspread==6.1.2
google-auth==2.34.0
